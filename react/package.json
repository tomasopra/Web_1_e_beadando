{
  "name": "react-apps-server",
  "version": "1.0.0",
  "description": "Szerver a React alkalmazások kiszolgálásához",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "cd todo && npm run build && cd ../expense-tracker && npm run build",
    "install-all": "npm install && cd todo && npm install && cd ../expense-tracker && npm install"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
} 